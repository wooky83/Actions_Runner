name: PYTEST

on:
  pull_request:
    branches: [ "main" ]

jobs:
  pytest:
    runs-on: [ self-hosted ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test with pytest
        run: |
          which pytest
          pytest python/ 
